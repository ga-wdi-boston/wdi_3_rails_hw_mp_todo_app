  <%= bootstrap_form_for ([@task, @task.notes.build]) do |f| %>
    <p>
      <%= f.text_field :note, autofocus: true, label: "Create Note" %>
    </p>
    <div class="button_slider_submit">
        <%= f.submit class: "btn btn-success"%>
    </div>
<% end %>
