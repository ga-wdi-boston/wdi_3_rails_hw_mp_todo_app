<%= form_for [@task, @note] do |f| %>
  <div class="field">
    <%= f.label 'note' %><br>
    <%= f.text_field :name, autofocus: true%>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-success move' %>
  </div>
<% end %>
