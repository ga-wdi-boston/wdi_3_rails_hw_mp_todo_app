    <%= form_for [@list, @task, @note] do |f| %>

    <p>
      <%= f.label :comment %>
      <%= f.text_field :comment%>
    </p>

    <%= f.submit %>
    <%= link_to "Cancel", list_tasks_path %>
    <% end %>
