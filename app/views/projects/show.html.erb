<h3><%= @project.title %></h3>

 <p><%= @project.due_date %></p>

 <p><%= @project.progress %></p>

<h2>Lists</h2>
<%= render @project.lists %>

<h2>Add a list:</h2>
<%= render "lists/form" %>

<%= link_to "All Projects", project_path(@project) %> |
<%= link_to "All Lists", '..lists/index_list_path'(@list) %>
<%= link_to "All Tasks", '..tasks/index_task_path'(@task) %>
<%= link_to "All Notes", '..notes/_note_note_path'(@note) %>
<%= link_to 'Edit', edit_project_path(@project) %>
