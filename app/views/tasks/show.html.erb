<p>
  <strong>Title:</strong>
    <%= @task.title %>
</p>


<h2> Add a note:</h2>
<%= form_for([@list, @task, @task.notes.build, *@note]) do |f| %>

<p>
  <%= f.label :body %>
  <%= f.text_area :body %>
</p>

<p>
  <%= f.submit %>
</p>

<% end %>
