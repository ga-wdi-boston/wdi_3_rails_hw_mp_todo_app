<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Completed:</strong>
  <%= @task.completed %>
</p>


<ul>
  <% @task.notes.each do |note| %>
  <li><%= note.comment %> </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_list_task_path(@list, @task) %> |
<%= link_to 'New Task', new_list_task_path(@list, @task)  %> |
<%= link_to 'New Note', new_list_task_note_path(@list, @task, @note)  %> |
<%= link_to 'All Notes', list_task_notes_path(@list, @task, @note)  %> |
<%= link_to 'Back', lists_path %>
