<p id="notice"><%= notice %></p>
<div class="navbar">
  <ul class ="nav nav-pills">
    <li><%= link_to 'Back to List', list_path(@list, @task)%> </li>
    <li><%= link_to 'Edit Task', edit_list_task_path(@list, @task) %> </li>
    <li><%= link_to 'New Note', new_list_task_note_path(@list, @task, @note)  %> </li>
  </ul>
</div>

<h2 class='header'><%= @task.name.capitalize%></h2>

<h3 class='header'>Completed?: <%= @task.completed %> </h3>
</p>

<h3>All Notes:</h3>

<% @task.notes.each do |note| %>
    <div class='panel col-xs-4 col-xs-offset-1'><h4><%= note.comment %></h4>
    <p class='btn btn-default'><%= link_to 'Edit', edit_list_task_note_path(note.task.list, note.task, note) %></p>
    <p class='btn btn-default'> <%= link_to 'Destroy', list_task_note_path(note.task.list, note.task, note), method: :delete, data: { confirm: 'Are you sure?' } %></p></div>
<% end %>


