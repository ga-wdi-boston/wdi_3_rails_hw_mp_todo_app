<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<h2>tasks</h2>
<%= render @list.tasks %>

<%= link_to 'New Task',  new_list_task(@task) %>
<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
