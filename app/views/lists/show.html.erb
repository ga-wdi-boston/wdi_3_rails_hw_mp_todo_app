<p id="notice"><%= notice %></p>

<div class='col-sm-6 col-xs-8 col-xs-offset-2 col-sm-offset-0 well well-lg text-center'>
  <h1><%= @list.name %></h1>
  <h2>Tasks</h2>

  <div>
    <%= link_to 'Edit', edit_list_path(@list) %> |
    <%= link_to 'Remove', list_path(@list), method: :delete, data: { confirm: 'Are you sure?' } %> |
    <%= link_to 'Back to all lists', lists_path %>
  </div>

</div>

<div class='col-sm-4 col-xs-8 col-xs-offset-2'>
<%= render @list.tasks.order(:id) %>
<%= render 'tasks/form' %>

